# Tool version pins — override via env before running install.sh or update.sh
# Example: KUBECTX_VERSION=0.9.6 bash install.sh

KUBECTX_VERSION="${KUBECTX_VERSION:-0.9.5}"
MAVEN_VERSION="${MAVEN_VERSION:-3.9.6}"
YQ_VERSION=4.52.4
AWSCLI_VERSION="${AWSCLI_VERSION:-2}"   # AWS CLI v2 (always latest v2)
